# Versiones pinchadas: mujoco y mink se rompen entre menores.
# El SDK `theker_telemetry` NO va aquí — lo instala scripts/setup.sh editable desde el
# repo Platform (rama dev), porque es el contrato de quien almacena el dato.
#
# Conjunto reconciliado con el del repo predecesor `STACKSPECT/Guionized-simulation`,
# que ya las tenía verificadas funcionando juntas. Donde los dos repos discrepaban, gana
# la versión que el predecesor probó. Todo va pinchado a `==`: lo que resuelva hoy tiene
# que resolver igual la noche de una demo.
mujoco==3.13.0
mink==1.3.0
# mink resuelve la IK con qpsolvers y NO trae solver propio: si falta el backend,
# solve_ik revienta en tiempo de ejecución. Se fijan los que instaló mink 1.3.0.
#
# qpsolvers es LGPL-3.0. Se usa como biblioteca y no se redistribuye dentro de este
# repo, así que no arrastra la licencia del proyecto (ver README, "Dependencies").
qpsolvers==4.13.0
daqp==0.9.1
numpy==2.4.4
# Geometría de la medida. Faltaba en este fichero aunque el predecesor ya la listaba:
# sin ella `src/measure.py` no se puede portar.
scipy==1.18.1
PyYAML==6.0.3
# Solo para escribir las PNG del palé.
imageio==2.37.3
